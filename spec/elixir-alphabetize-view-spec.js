'use babel';

import ElixirAlphabetizeView from '../lib/elixir-alphabetize-view';

describe('ElixirAlphabetizeView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
